{
  "name": "file-rename",
  "version": "1.0.0",
  "description": "파일 이름 일괄 변경 앱",
  "scripts": {
    "tauri": "cargo tauri",
    "tauri:dev": "cargo tauri dev",
    "tauri:build": "cargo tauri build",
    "build": "cargo tauri build && bash scripts/copy-to-dist.sh"
  },
  "devDependencies": {},
  "dependencies": {}
}
