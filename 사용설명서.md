# File Arrange for Fish Class - 사용 설명서

양양 물고기반 공유 폴더 업로드용 **이미지/동영상 파일 이름 일괄 변경** 앱입니다.  
카메라로 촬영한 파일을 물고기 종, 촬영 위치, 날짜 등으로 표준화된 이름으로 쉽게 변경할 수 있습니다.

---

## 목차

1. [설치 방법](#1-설치-방법)
2. [실행 방법](#2-실행-방법)
3. [배포 (설치 파일 만들기)](#3-배포-설치-파일-만들기)
4. [사용 방법](#4-사용-방법)
5. [파일 이름 규칙](#5-파일-이름-규칙)
6. [키보드 단축키](#6-키보드-단축키)
7. [문제 해결](#7-문제-해결)

---

## 1. 설치 방법

### 배포된 앱 사용 (권장)

- **macOS**: DMG 파일을 열어 앱을 응용 프로그램 폴더에 드래그

### 개발 환경에서 실행하려면

1. **Rust** 설치: [rustup.rs](https://rustup.rs/)
2. **Tauri CLI** 설치:

```bash
cargo install tauri-cli --version "^2"
```

---

## 2. 실행 방법

### 개발 모드로 실행

```bash
cargo tauri dev
```

### 설치형 앱으로 실행 (배포 후)

- **macOS**: `File Arrange for Fish Class.app` 실행 또는 DMG 설치 후 실행

---

## 3. 배포 (설치 파일 만들기)

### macOS용 빌드

```bash
npm run build
```

**생성 파일** (`dist/` 폴더):
| 파일 | 용도 |
|------|------|
| `macos/File Arrange for Fish Class.app` | macOS 앱 번들 (바로 실행 가능) |
| `dmg/File Arrange for Fish Class_1.0.0_aarch64.dmg` | 설치용 디스크 이미지 (Apple Silicon) |

> macOS 빌드는 macOS에서만 가능합니다.

### Windows용 빌드

Windows 환경에서 동일하게 `cargo tauri build` 실행 시 `.msi` 또는 `.exe` 설치 파일이 생성됩니다.

> Windows 빌드는 Windows에서만 가능합니다. Tauri는 크로스 컴파일을 지원하지 않습니다.

---

## 4. 사용 방법

### 4-1. 폴더 선택

1. **+ 폴더 선택** 버튼 클릭
2. 이미지/동영상이 있는 폴더 선택
3. JPG, PNG, MP4 등 지원 형식의 파일이 자동으로 로드됩니다

### 4-2. 기본 정보 입력 (선택)

상단 **기본 정보** 카드에서 다음을 미리 입력하면, 모든 파일에 공통으로 적용됩니다.

| 항목 | 설명 | 예시 |
|------|------|------|
| 지역/접두사 | 촬영 지역 접두사 | 남애, 북애, 동애, 서애, 속초, 고성 |
| 포인트이름 | 세부 지점 이름 | 밸리, 구선미 |
| 야간 | 야간 촬영 여부 | 체크 시 포인트 이름 뒤에 `N` 붙음 |
| 촬영자이름 | 촬영자 성명 | 홍길동 |
| 촬영일자 | 8자리 날짜 | 20250607 |

**오늘 날짜** 버튼을 누르면 당일 날짜가 자동 입력됩니다.

### 4-3. 파일별 물고기 이름 지정

1. 미리보기 화면에서 사진/동영상 확인
2. **물고기 이름** 입력란에 종명 입력 (예: 황점개볼락)
3. 필요 시 체크박스 선택:
   - **유어(J)**: 치어/유어 사진일 때 (파일명에 `J` 접미사)
   - **이름 불명확/모름**: 종을 특정하기 어려울 때 (괄호 처리)
4. **파일명 미리보기**에서 결과 확인
5. **Enter** 키 또는 다음 파일로 넘어가면 자동으로 파일 이름 변경

### 4-4. 스킵 (Skip)

분류하지 않을 파일은 **스킵** 버튼을 누르면 같은 폴더 내 `Skip` 폴더로 이동됩니다.

### 4-5. 확대 보기

- 이미지/동영상을 **클릭**하면 확대 뷰어가 열립니다
- **+ / −** 버튼으로 확대/축소
- **더블클릭**으로 1x ↔ 2x 토글
- **두 손가락 스퀴즈**로 확대/축소 (트랙패드)
- **ESC**로 닫기

---

## 5. 파일 이름 규칙

최종 파일명 형식:

```
{원본코드}_{물고기}_[지역접두사][포인트이름]_[촬영자]_[촬영일자].확장자
```

### 예시

| 입력 | 결과 |
|------|------|
| 원본: IMG_1234.jpg, 물고기: 황점개볼락, 포인트: 밸리, 촬영자: 김철수, 날짜: 20250607 | `IMG_1234_황점개볼락_남애밸리_김철수_20250607.jpg` |
| 유어 체크 | `IMG_1234_황점개볼락J_남애밸리_김철수_20250607.jpg` |
| 이름 불명확 체크 | `IMG_1234_(물고기)_남애밸리_김철수_20250607.jpg` |
| 야간 체크 | `IMG_1234_황점개볼락_남애밸리N_김철수_20250607.jpg` |

### 지원 포맷

- **이미지**: JPG, PNG, GIF, WebP, BMP, HEIC, HEIF, ORF, CR2, CR3, ARW, NEF, DNG 등
- **동영상**: MP4, MOV, AVI, MKV, WebM, M4V

> HEIC/HEIF 및 RAW 파일(ORF, CR2 등)은 미리보기가 불가하며 "미리보기 불가" 플레이스홀더가 표시됩니다. 파일 이름 변경은 정상 동작합니다.

---

## 6. 키보드 단축키

| 키 | 동작 |
|----|------|
| **Enter** | 파일명 변경 후 다음 파일로 이동 |
| **←** (좌화살표) | 이전 파일 |
| **→** (우화살표) | 다음 파일 |
| **ESC** | 확대 뷰어 닫기 |

---

## 7. 문제 해결

### macOS에서 "개발자를 확인할 수 없습니다" 경고

코드 서명 없이 빌드한 앱은 이 경고가 나타날 수 있습니다.

1. **시스템 설정** → **개인 정보 보호 및 보안**
2. "확인 없이 열 수 없는 앱" 섹션에서 해당 앱 옆 **열기** 클릭

### HEIC/RAW 파일이 미리보기에 안 보여요

HEIC, HEIF 및 카메라 RAW 포맷(ORF, CR2, ARW, NEF, DNG 등)은 미리보기를 지원하지 않습니다. "미리보기 불가" 플레이스홀더가 표시되며, **파일 이름 변경은 정상 동작**합니다.

### 기본 정보가 다음 실행 시에도 유지되나요?

네. 지역/접두사, 포인트이름, 촬영자, 촬영일자, 야간 여부는 앱 종료 후에도 저장됩니다.

---

## 버전 정보

- **앱 이름**: File Arrange for Fish Class
- **버전**: 1.0.0
- **프레임워크**: Tauri v2 (Rust + WebView)
- **용도**: 양양 물고기반 공유 폴더 업로드용 파일 이름 표준화
